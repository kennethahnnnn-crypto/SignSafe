# --- [기존 ClauseMate 필수 요소: 절대 삭제 금지] ---
flask
gunicorn
flask-login
flask-sqlalchemy
werkzeug
Pillow
pypdf
python-docx
google-generativeai

# --- [새로 추가되는 AI 변호사(RAG) 요소] ---
chromadb
python-dotenv
pydantic>=2.0
pydantic-settings

# --- [Render 배포 시 에러 방지용 특수 라이브러리] ---
# ChromaDB는 최신 SQLite를 요구하는데, Render 서버의 기본 SQLite 버전이 낮을 수 있습니다.
# 이 에러를 방지하기 위해 아래 라이브러리를 추가합니다.
pysqlite3-binary ; sys_platform == 'linux'